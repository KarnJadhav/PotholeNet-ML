# Lightweight, torch-free requirements for ML_BACKEND=onnx deployment
# (Render, Cloud Run, or anywhere RAM/image-size is constrained).
# For local dev with GPU + full Ultralytics API, use requirements.txt instead.

fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pillow==10.4.0
opencv-python-headless==4.10.0.84
numpy==1.26.4
python-dotenv==1.0.1
pydantic==2.9.2
huggingface_hub==0.25.1
onnxruntime==1.19.2
